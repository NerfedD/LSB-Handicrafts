# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright failure artifacts (traces, screenshots, zips)
test-results
playwright-report

# Environment variables (contains Supabase keys)
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Stray nested npm install, if it ever comes back
lsbHandicraft/

# Internal security audit / remediation notes. Kept out of the repo: this is
# a public GitHub project, and a list of live vulnerabilities is not something
# to publish alongside the code it describes.
BUGS_AND_FIX_PLAN.txt
*_AUDIT.txt
